<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>
        <variable
            name="viewModel"
            type="com.example.chatmessengerapp.mvvm.ChatAppViewModel" />
    </data>

    <FrameLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".fragments.SettingsFragment">

        <!-- OPTIONAL BACKGROUND IMAGE (same idea as Home, can be removed if ni vill) -->
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/sign_up_background"
            android:scaleType="centerCrop"
            android:alpha="0.15"
            android:contentDescription="@null" />

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <!-- TOOLBAR -->
                <androidx.appcompat.widget.Toolbar
                    android:id="@+id/toolbarMain"
                    android:layout_width="match_parent"
                    android:layout_height="?attr/actionBarSize"
                    android:background="@drawable/toolbar_gradient"
                    android:paddingHorizontal="8dp">

                    <ImageView
                        android:id="@+id/settingBackBtn"
                        android:layout_width="32dp"
                        android:layout_height="32dp"
                        android:src="@drawable/back"
                        android:layout_gravity="center_vertical" />
                </androidx.appcompat.widget.Toolbar>

                <!-- PROFILE IMAGE -->
                <de.hdodenhof.circleimageview.CircleImageView
                    android:id="@+id/settingUpdateImage"
                    android:layout_width="140dp"
                    android:layout_height="140dp"
                    android:layout_marginTop="32dp"
                    android:layout_marginBottom="16dp"
                    android:layout_gravity="center"
                    android:src="@drawable/person"
                    app:civ_border_width="2dp"
                    app:civ_border_color="#FE2C54" />

                <!-- TITLE -->
                <TextView
                    android:id="@+id/titleText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Update Your Profile"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    android:textColor="#222222"
                    android:layout_marginStart="16dp"
                    android:layout_marginEnd="16dp"
                    android:layout_marginBottom="24dp" />

                <!-- NAME INPUT (SAME STYLE AS SIGN IN / SIGN UP) -->
                <com.google.android.material.textfield.TextInputLayout
                    style="@style/FauxGlassOutlinedInput"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="16dp"
                    android:layout_marginEnd="16dp"
                    android:layout_marginBottom="24dp">

                    <com.google.android.material.textfield.TextInputEditText
                        android:id="@+id/settingUpdateName"
                        android:layout_width="match_parent"
                        android:layout_height="56dp"
                        android:hint="Name"
                        android:text="@={viewModel.name}"
                        android:inputType="textCapWords"
                        android:textColor="#222222"
                        android:textColorHint="#66000000"
                        android:importantForAutofill="no" />
                </com.google.android.material.textfield.TextInputLayout>

                <!-- UPDATE BUTTON -->
                <com.google.android.material.button.MaterialButton
                    android:id="@+id/settingUpdateButton"
                    android:layout_width="180dp"
                    android:layout_height="56dp"
                    android:layout_gravity="center"
                    android:text="Update"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@android:color/white"
                    android:backgroundTint="#6626C6DA"
                    app:cornerRadius="16dp"
                    app:strokeWidth="1dp"
                    app:strokeColor="#80FFFFFF"
                    android:elevation="6dp" />

                <!-- BOTTOM SPACING -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="40dp" />

            </LinearLayout>
        </ScrollView>

    </FrameLayout>

</layout>